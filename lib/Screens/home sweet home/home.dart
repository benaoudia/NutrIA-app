import 'package:flutter/material.dart';
Future<List<String>>> getNutrients() async {
    List<String>> nutients = await ; //await 
  return nutients
}
class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [
          Row(children: [

          ],)
        ],
      ),
    );
  }
}